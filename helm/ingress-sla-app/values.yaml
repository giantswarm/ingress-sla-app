name: ingress-sla-app
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/ingress-sla-app
  tag: v0.0.1
  pullPolicy: IfNotPresent
